# 智能翻译插件

> 基于腾讯云机器翻译的 Naimo Tools 插件，支持多语言实时翻译

## 功能特性

- ✅ 支持多种语言互译（中文、英语、日语、韩语等 14 种语言）
- ✅ 自动检测源语言
- ✅ 智能缓存翻译结果
- ✅ 支持快捷键操作
- ✅ 自动从剪贴板读取文本
- ✅ 一键复制翻译结果
- ✅ 自动翻译（输入后 1 秒自动触发）
- ✅ 优雅的界面设计

## 使用方法

### 1. 安装插件

1. 构建插件：

   ```bash
   npm install
   npm run build
   ```

2. 将插件目录复制到 Naimo Tools 的 `plugins` 目录
3. 重启 Naimo Tools

### 2. 配置 API 密钥

1. 在 Naimo Tools 中打开插件设置
2. 填写腾讯云 API 密钥：
   - **Secret ID**: 从腾讯云控制台获取
   - **Secret Key**: 从腾讯云控制台获取
   - **服务区域**: 选择最近的区域（如成都、北京等）

> 💡 **获取腾讯云 API 密钥**：访问 [腾讯云控制台](https://console.cloud.tencent.com/cam/capi)

### 3. 使用插件

#### 方式一：搜索框触发

- 在 Naimo Tools 搜索框中输入 `翻译` 或 `translate`
- 选择"翻译"功能
- 输入或粘贴要翻译的文本
- 点击"翻译"按钮或等待自动翻译

#### 方式二：快捷键

- `Ctrl/Cmd + Enter` - 执行翻译
- `Ctrl/Cmd + K` - 清空内容
- `Esc` - 隐藏窗口

#### 方式三：自动剪贴板

- 复制需要翻译的文本
- 打开翻译插件，会自动读取剪贴板内容

## 支持的语言

| 语言代码 | 语言名称 | 语言代码 | 语言名称 |
| -------- | -------- | -------- | -------- |
| auto     | 自动检测 | zh       | 中文     |
| en       | 英语     | ja       | 日语     |
| ko       | 韩语     | fr       | 法语     |
| de       | 德语     | es       | 西班牙语 |
| ru       | 俄语     | pt       | 葡萄牙语 |
| it       | 意大利语 | ar       | 阿拉伯语 |
| th       | 泰语     | vi       | 越南语   |

## 开发说明

### 技术栈

- **语言**: TypeScript
- **构建工具**: Vite
- **UI 框架**: 原生 HTML/CSS
- **翻译 API**: 腾讯云机器翻译
- **插件系统**: Naimo Tools Plugin API

### 项目结构

```
translate-plugin/
├── src/
│   ├── main.ts           # 主 UI 逻辑
│   ├── preload.ts        # Preload 脚本（翻译 API 调用）
│   └── style.css         # 样式文件
├── typings/
│   └── naimo.d.ts        # Naimo API 类型定义
├── _translate-plugin/    # 旧版本插件（参考）
├── dist/                 # 构建输出目录
├── index.html            # 主 HTML 文件
├── manifest.json         # 插件配置
├── preload.js            # 编译后的 preload 脚本
├── package.json          # 项目配置
├── tsconfig.json         # TypeScript 配置
├── vite.config.ts        # Vite 配置
└── README.md             # 说明文档
```

### 开发流程

1. **克隆项目**

   ```bash
   git clone <repository-url>
   cd translate-plugin
   ```

2. **安装依赖**

   ```bash
   npm install
   ```

3. **开发模式**

   ```bash
   npm run dev
   ```

4. **构建生产版本**

   ```bash
   npm run build
   ```

5. **类型检查**

   ```bash
   npm run type-check
   ```

6. **部署到 build 分支**

   ```bash
   npm run deploy
   ```

   该命令会自动完成以下操作：

   - 执行完整的项目打包
   - 删除远程 build 分支的所有内容
   - 将 dist 目录的内容推送到 build 分支
   - 自动生成部署提交信息（包含版本号和原始提交信息）

### API 说明

#### Naimo API

插件使用 `window.naimo` 访问 Naimo Tools 提供的 API：

- `naimo.storage` - 数据存储
- `naimo.clipboard` - 剪贴板操作
- `naimo.system` - 系统通知
- `naimo.window` - 窗口管理
- `naimo.log` - 日志记录

详细 API 文档请查看 `typings/naimo.d.ts`

#### 翻译 API

通过 `window.translatePluginAPI.translateText()` 调用翻译服务：

```typescript
const result = await window.translatePluginAPI.translateText({
  sourceText: "你好",
  source: "auto",
  target: "en",
  settings: {
    secretId: "YOUR_SECRET_ID",
    secretKey: "YOUR_SECRET_KEY",
    region: "ap-chengdu",
  },
});
```

## 常见问题

### 1. 翻译失败怎么办？

- 检查网络连接是否正常
- 确认腾讯云 API 密钥配置正确
- 检查腾讯云账户余额是否充足
- 查看控制台错误信息

### 2. 如何获取腾讯云 API 密钥？

1. 访问 [腾讯云控制台](https://console.cloud.tencent.com/)
2. 登录账号
3. 进入【访问管理】-【访问密钥】-【API 密钥管理】
4. 创建或查看现有密钥

### 3. 支持哪些翻译场景？

- 日常对话翻译
- 技术文档翻译
- 网页内容翻译
- 代码注释翻译
- 邮件翻译

### 4. 翻译速度慢怎么办？

- 选择离你更近的服务区域
- 检查网络连接速度
- 减少单次翻译的文本长度

## 版本历史

### v2.0.0 (2025-01-09)

- ✨ 重构为 TypeScript 项目
- ✨ 使用最新 Naimo Plugin API
- ✨ 改进 UI 界面设计
- ✨ 添加自动翻译功能
- ✨ 优化翻译速度和稳定性
- 🐛 修复各种已知问题

### v1.0.0

- 初始版本

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！

## 联系方式

- 作者：Naimo Tools Team
- 邮箱：support@naimo.tools
- 网站：https://naimo.tools

---

**享受智能翻译的便利！** 🚀
